<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Summarizer</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex flex-col items-center justify-center">
        <div class="max-w-md w-full bg-white rounded-lg shadow-lg p-8 space-y-6">
            <h1 class="text-3xl font-bold text-center text-gray-800">Email Summarizer</h1>
            <p class="text-center text-gray-600">Get daily summaries of your unread emails directly in your inbox!</p>
            
            <div class="space-y-4">
                <% if (typeof error !== 'undefined' && error === 'auth_failed') { %>
                    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
                        Authentication failed. Please try again.
                    </div>
                <% } %>
                
                <a href="/auth/gmail" 
                   class="w-full flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-base font-medium text-white bg-blue-600 hover:bg-blue-700">
                    Sign in with Gmail
                </a>
            </div>

            <div class="mt-8 text-sm text-gray-500">
                <h2 class="font-semibold mb-2">How it works:</h2>
                <ul class="list-disc pl-5 space-y-1">
                    <li>Sign in with your Gmail account</li>
                    <li>Choose where to receive your summaries</li>
                    <li>Get daily digests of unread emails</li>
                    <li>Stay organized without constant email checking</li>
                </ul>
            </div>
        </div>

        <footer class="mt-8 text-center text-gray-500 text-sm">
            <p>Your data is secure. We only request read-only access to your emails.</p>
        </footer>
    </div>
</body>
</html> 